{
  "name": "reconstruct",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source application reconstruction engine for AI coding agents",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "reconstruct": "pnpm --filter @reconstruct/cli exec reconstruct"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
